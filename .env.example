# AI-Army – copy to .env (development) or .env.production (production)
# Production .env.production is injected into the Docker container on deploy.

# Anthropic API key for Claude (required)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Anthropic model for crews and chains. Default: claude-haiku-4-5
# ANTHROPIC_LLM_MODEL=claude-haiku-4-5

# GitHub: use a Personal Access Token (PAT) with repo scope for API access.
# Single repo:
GITHUB_TARGET_TOKEN=your_github_pat_here

GITHUB_TARGET_REPO=owner/repo-name

# Multi-repo (optional):
# GITHUB_REPO_1=owner/repo-one
# GITHUB_TOKEN_1=pat_for_repo_one
# GITHUB_REPO_2=owner/repo-two
# GITHUB_TOKEN_2=pat_for_repo_two

# RAG embedding model for codebase search (dev agents). Default: all-MiniLM-L6-v2
# RAG_EMBEDDING_MODEL=all-MiniLM-L6-v2

# Local clone for dev crew (branch, commit, push)
# LOCAL_REPO_PATH=/path/to/cloned/repo

# LangSmith tracing (optional) - trace LangChain calls at smith.langchain.com
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_API_KEY=your_langsmith_api_key
