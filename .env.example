# AI-Army Configuration
# Copy this file to .env (development) and .env.production (release/deploy)
# - .env: used by default for local development (scripts/start.py)
# - .env.production: used by release and deploy scripts

# Anthropic API key for Claude (required for all agents)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# GitHub Personal Access Token (repo, issues, pull_requests scopes)
GITHUB_TOKEN=your_github_token_here

# Target repository in owner/repo format (e.g. myorg/my-app)
GITHUB_TARGET_REPO=owner/repo-name

# Optional: OpenAI API key for fallback tools
# OPENAI_API_KEY=your_openai_key_here

# --- Deploy (scripts/deploy, .env.production) ---
# DO_DROPLET_HOST=your-droplet-ip-or-hostname
# DO_DROPLET_USER=root
# DO_SSH_KEY_PATH=~/.ssh/your-key
# APP_NAME=ai-army
